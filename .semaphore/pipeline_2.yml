version: v1.0
name: Pipeline 2
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu1804
blocks:
  - name: Deployment
    task:
      jobs:
        - name: Storybook publish
          commands:
            - npm run chromatic --if-present
      env_vars:
        - name: CHROMATIC_PROJECT_TOKEN
          value: djrksgh3ale
